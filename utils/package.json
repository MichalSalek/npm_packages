{
  "private": false,
  "name": "@msalek/utils",
  "version": "1.0.3",
  "description": "",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cleanup": "rm -rf ./data ./esm index.js index.d.ts index.js.map",
    "b": "yarn cleanup && yarn build:esm && yarn build:cjs",
    "build:esm": "tsc --project tsconfig.json --outDir esm",
    "build:cjs": "tsc --project tsconfig.json --module commonjs",
    "release:major": "yarn b && npm version $(semver $npm_package_version -i major) && npm publish --tag latest",
    "release:minor": "yarn b && npm version $(semver $npm_package_version -i minor) && npm publish --tag latest",
    "release:patch": "yarn b && npm version $(semver $npm_package_version -i patch) && npm publish --tag latest",
    "npm-auth": "npm adduser"
  },
  "author": "Michał Sałek <contact@michalsalek.com> (michalsalek.com)",
  "license": "BSD-1-Clause",
  "dependencies": {},
  "devDependencies": {
    "semver": "^7.3.7",
    "@types/react": "^18.2.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.0.4",
    "@types/node": "^20.2.3",
    "tslib": "^2.5.2"
  }
}
